# slicing/selecting sub sequences
# to print more than one index
# name[start argument : stop argument+1]

name="Abrar"
print(name[1:4])
print(name[:]) # we will get full string
print(name[1:]) # we will get b to r
print(name[:3]) # we will get A to r